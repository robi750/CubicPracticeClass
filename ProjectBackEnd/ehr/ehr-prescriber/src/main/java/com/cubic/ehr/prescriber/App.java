package com.cubic.ehr.prescriber;

import java.text.ParseException;
import java.text.SimpleDateFormat;
import java.time.LocalDate;
import java.time.ZoneId;
import java.util.Date;


public class App 
{

	
    public static void main( String[] args ) throws ParseException{
    	
    	String s = null;
    	if(s==null) {
    		
    	}
    	/*	String dateStr = "12/26/1999";
		Date dob = new SimpleDateFormat("MM/dd/yyyy").parse(dateStr);
		
			Date current = new Date();
			LocalDate localDate = current.toInstant().atZone(ZoneId.systemDefault()).toLocalDate();
			LocalDate ageDate = dob.toInstant().atZone(ZoneId.systemDefault()).toLocalDate();
			System.out.println((localDate.getYear()-ageDate.getYear()));
			
			System.out.println((localDate.getYear()-ageDate.getYear()) >= 2 && (localDate.getYear()-ageDate.getYear())<=60);
*/			
    }
}
